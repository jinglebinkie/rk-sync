apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "rk-sync-helm.fullname" . }}-pvc
  labels:
    {{- include "rk-sync-helm.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
